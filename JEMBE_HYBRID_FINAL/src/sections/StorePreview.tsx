import { Link } from "react-router-dom";import { ArrowUpRight } from "lucide-react";import SectionHeading from "../components/SectionHeading";import ProductCard from "../components/ProductCard";import FadeIn from "../components/FadeIn";import { useProductsStore } from "../hooks/useContentStore";import { useSiteContent } from "../hooks/useSiteContent";
export default function StorePreview(){const site=useSiteContent();const {items}=useProductsStore();const featured=items.filter(p=>p.status==="live"&&p.featured).slice(0,3);return <section data-jembe-section-id="store" id="store-preview" className="py-24 md:py-32 border-t border-stroke"><div className="max-w-content mx-auto px-6"><div className="flex flex-col md:flex-row md:items-end justify-between gap-8 mb-14"><SectionHeading label={site.storeLabel} heading={site.storeHeadline} description={site.storeDescription}/><FadeIn delay={.2}><Link to="/store" className="focus-ring accent-ring inline-flex items-center gap-2 border border-stroke rounded-full px-5 py-2.5 text-sm shrink-0">View full store <ArrowUpRight size={14}/></Link></FadeIn></div><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">{featured.map((product,i)=><FadeIn key={product.id} delay={i*.08}><ProductCard product={product}/></FadeIn>)}</div></div></section>}
